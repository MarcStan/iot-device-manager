{
  "ConnectionStrings": {
    // 1. run: az iot hub device-identity show-connection-string --hub-name {IoTHubName} --device-id {deviceName} --output table
    // 2.a store with: dotnet user-secrets set "ConnectionStrings:DeviceConnection" {connectionString}
    // 2.b alternatively rightclick the project, select "Manage User Secrets" and paste the value at the correct location into the json
    "DeviceConnection": ""
  }
}
