{
  "ConnectionStrings": {
    // run: az iot hub show-connection-string --hub-name {IoTHubName} --output table
    // and store the connectionstring here
    // if you are debugging from within Visual Studio. rightclick the project, select "Manage User Secrets" and paste the value at the correct location into the json
    "IoTHub": ""
  }
}
